# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-ITpp
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND ITpp_sources
    BCH_Decoder_impl.cc
    BCH_Encoder_impl.cc
    Hamming_Decoder_impl.cc
    Hamming_Soft_Decoder_impl.cc
    Hamming_Encoder_impl.cc
    RS_Decoder_impl.cc
    RS_Encoder_impl.cc
    BSC_Byte_impl.cc
    BER_Analyzer_cpp_impl.cc
)

set(ITpp_sources "${ITpp_sources}" PARENT_SCOPE)
if(NOT ITpp_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT ITpp_sources)

add_library(gnuradio-ITpp SHARED ${ITpp_sources})
target_link_libraries(gnuradio-ITpp gnuradio::gnuradio-runtime)
target_link_libraries(gnuradio-ITpp ${ITPP_LIBRARY})
target_include_directories(gnuradio-ITpp
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
  )
set_target_properties(gnuradio-ITpp PROPERTIES DEFINE_SYMBOL "gnuradio_ITpp_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-ITpp PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-ITpp)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_ITpp_sources
qa_BCH_Decoder.cc
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-ITpp)

if(NOT test_ITpp_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_ITpp_sources)

foreach(qa_file ${test_ITpp_sources})
    GR_ADD_CPP_TEST("ITpp_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
